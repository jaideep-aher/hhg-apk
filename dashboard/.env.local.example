# Firebase Admin SDK credentials
# Firebase Console → Project Settings → Service Accounts → Generate new private key
# Copy the values from the downloaded JSON file into these vars.

FIREBASE_PROJECT_ID=hhg-apk
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@hhg-apk.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"

# Simple password to protect the dashboard
DASHBOARD_PASSWORD=change-me

# Shared secret for the /api/push/send endpoint. The /send UI asks for this
# in-session (it isn't baked into the bundle). Rotate by changing this var
# and redeploying; no client changes required.
PUSH_ADMIN_SECRET=change-me-to-a-long-random-string

# (Optional) Postgres-backed HHG Farmers API origin, e.g.
#   BACKEND_URL=https://api.hanumanksk.in
# When set, the dashboard enriches each Firestore farmer row with
# name / phone / address from the backend's GET /api/farmer/:uid and
# enables name-search on the dashboard. Leave blank to run id-only.
BACKEND_URL=

# (Optional) Force enrichment for every /api/farmers request even when the
# client doesn't ask for it via ?enrich=1. Only meaningful with BACKEND_URL set.
ENABLE_NAME_ENRICHMENT=
